{
  "name": "nahr-proposal-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node scripts/check.js",
    "test:live": "playwright test tests/live-generator.spec.js --reporter=line"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
